# 維護規則

本專案是靜態內容索引與學習單網站。修改前閱讀 [README.md](README.md)；品質規則的正式說明在 [docs/quality.md](docs/quality.md)，可執行門檻以 [quality-policy.json](quality-policy.json) 為準。

- 索引來源只有 `worksheets.json`；分類來源只有 `categories.json`。不要手動修改 `data/` 或各集 `metadata.json`，請執行產生程式。
- 首頁只載入精簡目錄，搜尋時才載入搜尋詞。不要把文章全文、學習單 HTML 或全站字型預先載入首頁。
- 每個頁面只引用自己的精簡字型。文字變更後重建該頁字型；不得改用原始大型字型或把字型預算提高來通過檢查。
- 缺字必須檢查實際使用的字重。優先換成不改變題意的相近詞；保留原詞供搜尋。若替換會改變專有名稱、引述、分類意義或題目判斷，保留原意並記錄具體字型備援例外，不得自動改寫讀者作答。
- 列印溢出應重新分頁或調整合理的間距，保留題目與作答空間。不得隱藏超出內容、刪減問題、無限縮字或略過驗證。
- 內容與共用程式修改後執行產生程序、完整測試及建置。瀏覽器測試必須實際執行；靜態檢查不能代替列印、缺字與容量測試。
- 不得為了通過檢查直接更新例外清單或放寬預算。先解決原因；必要的例外必須提供確切文字、範圍與理由，並在交付時說明。
- 保留現有作答欄位 ID，除非欄位意義已改變。網站發布與 Git 推送須在使用者授權範圍內進行。

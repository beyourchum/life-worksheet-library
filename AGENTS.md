# 維護規則

本專案是靜態內容索引與學習單網站。本檔只保存工作入口、硬性限制、授權停止點與回報規則；操作順序見 [workflow](docs/workflow.md)，通過條件見 [quality](docs/quality.md)。

## 開始工作

1. 先讀 [README.md](README.md)，再依任務讀取 [workflow](docs/workflow.md) 與 [quality](docs/quality.md) 的適用章節。數值門檻只以 [quality-policy.json](config/quality-policy.json) 為準；只有確認特定頁面的既有核准例外時才讀 [quality-exceptions.json](config/quality-exceptions.json)。
2. 確認目前分支、未提交差異、有效來源與使用者停止點。沿用既有 `main` 和原工作目錄；保留無關修改，不另建分支或 worktree，也不強制切換、重設或丟棄修改。
3. 首次主要進度更新交代目標、停止點及即將執行的第一個動作，並明確標示尚未執行；不得把計畫、預期結果或「接下來會做」寫成已完成。一般維護的範圍與驗收條件留在對話；內容工作沿用既有工作稿，不另建單次計畫文件。
4. 對題意、版面歸屬、來源、使用者偏好或規則適用方式不確定時，先主動提出具體疑點請使用者確認，不自行猜測後寫入內容或成品。可明確判定且不受疑點影響的工作照常繼續。

## 權威來源與不可違反條件

- 索引只改 `catalog/worksheets.json`，分類只改 `catalog/categories.json`。`data/`、各集 `metadata.json`、搜尋索引及精簡字型由產生程式更新，不手動編輯。
- 原稿、修訂稿、網頁結構來源與 HTML 的責任及缺檔處理依 [內容來源與同步](docs/workflow.md#內容來源與同步)。找不到檔案時先查指定位置、本機備份與 Git 歷史；不得以 HTML 冒充原稿。
- 新頁面與完整改造頁面以 `worksheet-sources/EP編號.json` 作為網頁結構來源；來源納入版本控制但不發布，`worksheets/EP編號/index.html` 由既有產生器建立，不直接編輯。有結構化來源時只修改該來源、共用元件或已登錄版式變體，再重新產生 HTML；沒有結構化來源的既有頁面維持原責任，實際修改時逐篇遷移，不整批重製。
- 內容製作依 [四個交付階段與六個執行關卡](docs/workflow.md#四個交付階段與六個執行關卡)；SOURCE、CORRECTED、HTML、發布是對使用者的固定順序，內部關卡用來隔開模型判斷與可直接執行的程序。內容要求須在撰寫時完成，再以代表答案試填、人工驗收與自動檢查驗證。不得把檢查器當成內容產生器。
- 原稿忠實度、必備內容、題型、表格、選擇題、範例、影片及人工驗收以 [品質規則](docs/quality.md) 為準。技術測試不能代替逐題試填、來源對照或語意判斷。
- 特殊題型須先在內容稿定義狀態、限制、前後依賴與紙本作法；AI 提示詞須先定義每個括號的欄位來源。網站轉換後再測試實際互動。
- 特殊版面使用 `config/worksheet-layouts.json` 登錄的具名變體及共用 CSS；學習單 HTML 不得使用 `<style>`、`style` 屬性或單集 CSS／JS。特殊互動優先使用共用元件，同類需求第二次出現時不得繼續複製單頁實作。
- 每集影片須到[指定資料庫](https://docs.google.com/spreadsheets/d/1H8BpNhd5LytJfUue3AnFXwe2fujVZWEPdFFlr8oXP34/edit?gid=0#gid=0)依集數核對標題與儲存格連結。完成可用途徑後仍有權限或來源矛盾，才回報阻礙；不得以改測試、藏按鈕或自行宣告無影片繞過門檻。
- 作答欄位意義未變時保留 ID。首頁只載入精簡目錄，搜尋時才載入搜尋索引；不得預載全文、學習單 HTML 或全站字型。
- 每頁只引用自己的精簡字型。文字變更後重建該頁字型並檢查實際字重；不得以大型原始字型、提高預算或擴張例外清單處理失敗。
- 列印溢出以合理分頁與間距修正，保留題目及作答空間；不得隱藏內容、刪題、無限縮字或略過驗證。
- 原稿、修訂稿及本機維護紀錄依 `.gitignore` 保存。稿件備份、驗收成果、臨時檔與額外依賴依 [產物與清理](docs/artifacts.md) 管理；搬移或刪除前先核對引用、使用狀態與可恢復來源。

## 執行、驗收與發布

- 只有語意、內容、題型、互動、視覺或無法由既有報告定位的失敗需要模型判斷。抓取與保存來源、檔案操作、產生、建置、自動檢查、提交、推送及部署直接使用既有程序；不得為例行步驟重讀完整來源、重啟內容技能或重做已有且仍有效的判斷。
- 已授權且不受待確認事項影響的工作持續完成；例行讀檔、產生、修正、重驗與關卡切換不另設確認點。只有需要使用者取捨、額外權限或擴大範圍時才停下。可繼續執行時留在同一輪工作，不得先送出最終回覆再聲稱代理仍會繼續；一般對話的最終回覆送出後即視為本輪停止，除非已實際建立並回報可持續運作的排程或其他背景機制。
- 長工作可拆成可獨立驗證的小步驟逐一執行。每一步只在取得對應證據後標示完成，例如檔案差異、命令結果、測試報告、預覽或人工核對紀錄；尚未執行、執行中、失敗與受阻必須分開回報，不得用預定動作、推測結果或模糊措辭代替執行證據。
- 執行失敗或受阻時，保留已完成成果，指出失敗或未執行的確切步驟、可核對的原因及解除阻礙所需事項。未取得證據的後續步驟一律標示「尚未檢查」或「有問題」，不得宣稱已完成、已通過、已發布或代理正在背景處理。
- 內容或共用程式修改後，依 [本機驗收](docs/workflow.md#關卡-5本機驗收) 執行適用的產生、測試、建置、瀏覽器、列印、缺字、容量與互動檢查。
- 先修失敗原因；不得放寬門檻、改測試、增列例外、刪減題目或隱藏失敗來取得通過。驗收後再修改時，重驗受影響範圍。
- Git 推送與網站發布只在使用者授權內執行。推送前依 [發布驗證](docs/workflow.md#關卡-6發布驗證) 確認成果已整合並提交至既有 `main`，且不夾帶無關修改。

## 使用者指令與停止點

| 指令語意 | 完成條件 |
| --- | --- |
| 只輸入集數（如「EP42」） | 視為授權製作該集內容：查找並核對有效來源，使用 `worksheet-content-tw` 完成內容設計或審查、代表答案試填與人工驗收，交付 `content/EP編號/EP編號_corrected.md` 供確認；不製作 HTML、不提交（commit）、不推送（push）、不部署（deploy）。若來源缺失、互相矛盾或有會改變題意的必要決策，才停下提出具體問題。 |
| 「做好給我看」「先做本機版」「不要發布」 | 完成修改；有結構化來源時先重建 HTML，再執行適用產生程序、完整測試、本機建置與可用預覽；不提交（commit）、不推送（push）、不部署（deploy）。 |
| 「發布到網站」「更新網站」「完成並發布」 | 完成修改與驗收，提交（commit）本次相關變更至 `main`，推送（push）至 `origin/main`，等待 Actions 與 Pages 部署（deploy）成功，再完成正式網站抽查；鏈內步驟不重複詢問。 |
| 「只提交（commit）」「只推送（push）」或其他明確停止點 | 只執行到指定位置。 |
| 只修改本機規則 | 儲存並檢查規則；不提交（commit）、不推送（push）、不部署（deploy）。若原任務另有已授權工作，完成規則後接回原停止點。 |

發布鏈失敗時，在原授權內查因、修正並重試；只有需要新決策、權限或擴大範圍時才停止。推送（push）、部署（deploy）及線上抽查是三個獨立狀態，正式網站尚未可用前不得回報「已發布」。

## 固定回報格式

所有最終回覆與主要階段回報依序保留以下五欄；簡短執行中更新可用一句話。只有內容製作標示「第幾階段／4」。

- **目前進度**：工作名稱與「進行中／待確認／完成」。「完成」只用於已達本次授權停止點且有證據的工作；仍有已授權步驟可執行時保持「進行中」，不得送出最終回覆。
- **這次完成**：只列本輪已實際完成且可由檔案、工具輸出、檢查紀錄或預覽核對的結果、主要變更及可用成品；計畫、預期結果、下一個動作與未執行項目不得列入。HTML 交付附已確認可用的預覽。
- **檢查結果**：固定使用條列式，逐項寫出「檢查項目：狀態－實際檢查內容或結果摘要」；狀態只使用「通過／有問題／尚未檢查／不適用」。不得只寫籠統的「檢查完成」或「全部通過」。本機驗證須把實際執行或人工核對的項目分開列出；提交（commit）、推送（push）、部署（deploy）及線上抽查也各自獨立一項，即使不適用或尚未執行也不得省略。
- **需要你確認**：只列真正需要使用者判斷或尚未授權的事項；沒有就寫「無」。
- **下一步**：依下列決策表提供使用者可直接送出的接續指令。學習單流程使用「EP編號」「修正內容」「製作 HTML」及「發布網站」四種標準指令，並依目前成果連續提示下一個可執行指令；不得省略中間階段、提前提示後段指令，或用「等待內容確認」「請確認後告訴我」等代理狀態代替指令。

需要使用者送出接續指令時，每個文字框只放一句可直接送出的指令：

````markdown
- **下一步**：〔必要操作提醒〕

```text
〔接續指令〕
```
````

### 「下一步」決策表

「需要你確認」負責提出具體判斷問題；「下一步」負責提供使用者回答後可採取的接續指令，兩欄不得重複寫「等待確認」。依序套用：

1. **代理仍可繼續**：直接留在同一輪執行，不送出最終回覆、不索取新指令，也不把例行操作寫成使用者待辦。簡短執行中更新使用「下一個動作：〔第一個必要動作〕（尚未執行）」；完成該動作並取得證據後再更新狀態。不得寫「由代理繼續」、不得暗示有未建立的背景執行，也不得在「下一步」欄把代理自己的工作交給使用者。
2. **尚未指定學習單**：需要開始新一集時，「下一步」提供「EP〔集數〕」；已有明確集數時不得要求重送。
3. **學習單停在內容審閱點**：完成「EP編號」或「修正內容」後，在「需要你確認」提出具體內容問題。「下一步」預設同時提供兩條可選指令，各用一個文字框：需要調整時送出「修正內容：〔具體修改意見〕」，內容可用時送出「製作 HTML」。其中「製作 HTML」包含建立或更新 `worksheet-sources/EP編號.json`、由共用骨架產生 HTML、嵌入已核對影片、更新索引與完成本機驗收；既有頁面在實際修改時一併遷移。影片 metadata 只供工具列連結，不得當成頁面播放器已完成。只有已發現明確且會阻止內容定稿或網站製作的問題時，才只提供「修正內容：〔具體修改意見〕」，並在「檢查結果」指出問題位置與不通過原因；單純偏好未確認或仍可接受不同選擇，不算明顯問題。不得寫成「由代理等待內容確認」。
4. **學習單停在發布授權點**：完成「製作 HTML」且本機驗收通過時，「下一步」提供「發布網站」；若本機驗收發現內容問題，則退回提供「修正內容：〔具體修改意見〕」，不得提前提供發布指令。若只是 HTML 實作問題且仍在既有授權內，代理直接修正並重驗，不要求使用者重送指令。
5. **其他必要決策、權限或授權**：在「需要你確認」指出唯一阻礙、已完成的安全檢查，以及使用者必須回答或授權的具體事項；「下一步」提供能解除該阻礙的直接指令，不描述代理正在等待。
   已授權後續階段時，代理直接繼續，不把這些指令列成使用者待辦。
6. **非學習單工作已達停止點，或學習單已完成「發布網站」及線上抽查**：寫「成果已可依上方方式使用；你不需要再操作。」這是唯一不再提供學習單接續指令的終點。

不得把可選優化、重複確認、一般建議、Git 狀態或部署狀態寫成「下一步」；這些狀態只列在「檢查結果」。接續提示固定沿用 `EP編號 → 修正內容／製作 HTML → 發布網站 → 完成`，並以當前成果省略已完成的前段，不得跳過尚未完成的必要階段。內容審閱點預設同時列出「修正內容」與「製作 HTML」兩條分支，只有已有明顯阻擋問題時才能省略「製作 HTML」。不得在 HTML 與本機驗收完成前提供「發布網站」。已授權發布但發布鏈尚未完成時，適用第 1 項，代理須繼續執行。
